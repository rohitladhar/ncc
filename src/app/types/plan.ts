export type PlanType = {
  // type: number;
  slide: {
    one: string;
    two: string;
  };
  desc: {
    one: string;
    two: string;
  };
  option: {
    one: string[];
    two: string[];
  };
};
